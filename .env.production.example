# =============================================================================
# SurveyBot Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and configure for your VPS deployment:
#   cp .env.production.example .env.production
#   nano .env.production
#
# IMPORTANT: Never commit .env.production to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Telegram Bot Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your bot token from @BotFather on Telegram:
# 1. Open Telegram and search for @BotFather
# 2. Send /newbot command
# 3. Follow the prompts to create a new bot
# 4. Copy the token provided (format: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz)
BOT_TOKEN=your_telegram_bot_token_here

# Webhook secret for security (generate with: openssl rand -hex 32)
# This secret is sent in webhook requests to verify they're from Telegram
WEBHOOK_SECRET=generate_a_random_secret_here

# -----------------------------------------------------------------------------
# Domain Configuration (REQUIRED for production)
# -----------------------------------------------------------------------------
# Your domain name pointing to this server
# Examples:
#   - DuckDNS: mysurveybot.duckdns.org
#   - Custom domain: surveys.example.com
#   - IP only (HTTP): 123.45.67.89 (not recommended - no HTTPS)
DOMAIN=your-domain.duckdns.org

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL password - CHANGE THIS for production!
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=change_this_secure_password

# Database connection string is auto-configured in docker-compose.production.yml
# Only change these if using an external database:
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=surveybot_db
# POSTGRES_USER=surveybot_user

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Set to Production for VPS deployment
ASPNETCORE_ENVIRONMENT=Production

# -----------------------------------------------------------------------------
# JWT Authentication (Optional - has defaults)
# -----------------------------------------------------------------------------
# Secret key for JWT token signing (minimum 32 characters)
# Generate with: openssl rand -base64 48
# If not set, a default development key is used (insecure for production!)
# JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long

# Token lifetime in hours (default: 24)
# JWT_TOKEN_LIFETIME_HOURS=24

# =============================================================================
# QUICK SETUP CHECKLIST
# =============================================================================
# [ ] Get bot token from @BotFather
# [ ] Generate webhook secret: openssl rand -hex 32
# [ ] Get a domain (DuckDNS is free: https://www.duckdns.org)
# [ ] Point domain to your VPS IP address
# [ ] Change POSTGRES_PASSWORD
# [ ] (Optional) Generate custom JWT_SECRET
# =============================================================================
